import styled from 'styled-components'

export const Container = styled.div`
  max-width: 90%;
  max-height: 700px;
  height: 520px;
  margin-top: 70px;
  background: var(--bg-portal);
  color: white;
  border-radius: 8px;
  > p {
    align-items: center;
    padding: 3px;
    color: var(--bg-dark);
  }
`
