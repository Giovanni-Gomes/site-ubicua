import styled from 'styled-components';

export const Container = styled.div`
    display: flex;
    gap: 1rem;
    justify-content: center;
    padding: 1rem 0;

    label {
        color: black;
    }
`;
