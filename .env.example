# Copy to .env.local if you need optional third‑party keys (Mapbox for the map).
# The app API layer is fully mocked — no backend URL is required.

# Mapbox public token — https://account.mapbox.com/ (optional; map tiles need a valid token)
VITE_REACT_APP_MAPBOX_TOKEN=pk.your_mapbox_public_token_here
